class P
{
    static void Main()
    {
        for (
            int Y = 16, X = 0;
            Y++ < 71;
            X += (Y > 29 ? Y > 35 ? 556 : 524 : 247) << 5
        ) System.Console.Write("20{0}	{1}\n", Y, X += X/118);
    }
}
