interface A {
    static void main(String[]a) {
        for (
            int Y = 15, X = 0;
            Y++ < 70;
            X += Y > 28 ? 524 + Y / 35 * 32 << 5 : 7904
        ) System.out.format("20%d	%d\n", Y, X += X / 118);
    }
}
