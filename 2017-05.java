interface A {
    static void main(String[]a) {
        for (
            float Y = 16, X = 0;
            Y++ < 71;
            X += Y > 29 ? Y > 35 ? 17800 : 16800 : 7900
        ) System.out.format("20%f	%f\n", Y, X *= 1.00847);
    }
}
