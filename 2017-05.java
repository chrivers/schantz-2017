interface E {
    static void main(String[]a) {
        for (
            long Y = 16, X = 0;
            Y++ < 71;
            X += (Y > 29 ? Y > 35 ? 178 : 168 : 79) * 100
        ) System.out.format("20%d	%d\n", Y, X *= 1.00847);
    }
}
